{
    "publisher": "marian",
	"name": "stexls-vscode-client",
	"repository":{
		"type": "git",
		"url": "https://gl.kwarc.info/Marian6814/stexls-vscode-client"
	},
	"displayName": "stexls-vscode-client",
	"description": "VSCode client for the stex language server",
	"version": "1.0.3",
	"engines": {
		"vscode": "^1.44.0"
	},
	"categories": [
		"Other"
	],
	"activationEvents": [
		"onLanguage:latex"
	],
	"main": "./out/extension.js",
	"contributes": {
		"configuration":{
			"title": "stexls",
			"properties": {
				"stexls.pythonInterpreter": {
					"type": "string",
					"default": "python3",
					"description": "The python executable used to starts the server. The selected interpreter must have the stexls package installed. This setting can be the complete path to the interpreter executable."
				},
				"stexls.stexlsCommand": {
					"type": "array",
					"default": [
						"-m", "stexls", "lsp", "--transport-kind", "ipc", "--loglevel", "error", "--logfile", "/tmp/stexls.log"
					],
					"description": "The command executed by the selected python interpreter. This should start the language server. Go to a terminal and type 'python -m stexls lsp --help' for more information. The 'transport-kind' argument should never be changed."
				}
			}
		}
	},
	"scripts": {
		"vscode:prepublish": "npm run compile",
		"compile": "tsc -p ./",
		"lint": "eslint src --ext ts",
		"watch": "tsc -watch -p ./",
		"pretest": "npm run compile && npm run lint",
		"test": "node ./out/test/runTest.js"
	},
	"devDependencies": {
		"@types/vscode": "^1.44.0",
		"@types/glob": "^7.1.1",
		"@types/mocha": "^7.0.2",
		"@types/node": "^13.11.0",
		"eslint": "^6.8.0",
		"@typescript-eslint/parser": "^2.26.0",
		"@typescript-eslint/eslint-plugin": "^2.26.0",
		"glob": "^7.1.6",
		"mocha": "^7.1.1",
		"typescript": "^3.8.3",
		"vscode-test": "^1.3.0"
	},
	"dependencies": {
		"vscode-languageclient": "^6.1.3"
	}
}
